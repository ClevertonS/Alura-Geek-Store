generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id String @id @default(uuid())
  title String 
  category String
  productImage String
  description String
  price Decimal

  @@map("Products")
}
